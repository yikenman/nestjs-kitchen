export const DEFAULT_PASSPORT_PROPERTY_VALUE = 'user';

export const PASSPORT_PROPERTY = Symbol('PASSPORT_PROPERTY');

export const PREFIX = '`<`:';

export const ROUTES_OPTIONS = Symbol('ROUTES_OPTIONS');

export enum JwtValidationType {
  JWT = 0,
  REFRESH = 1
}

export const SESSION_PASSPORT_KEY = 'passport';
